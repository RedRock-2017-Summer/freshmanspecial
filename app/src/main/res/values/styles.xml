<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Popupwindow">
        <item name="android:windowEnterAnimation">@anim/window_out</item>
        <item name="android:windowExitAnimation">@anim/window_back</item>
    </style>

    <style name="Theme.ActivityDialogStyle" parent="Theme.Design.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowCloseOnTouchOutside">false</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <!--外距-->
    <dimen name="margin">10dp</dimen>
    <dimen name="margin_small">5dp</dimen>

    <!--TextSize-->
    <dimen name="size">20sp</dimen>
    <dimen name="size_small">15sp</dimen>

    <!--CardView的Radius-->
    <dimen name="radius">10dp</dimen>
    <dimen name="radius_small">5dp</dimen>

    <!--item_divider-->
    <dimen name="divider">10dp</dimen>

</resources>
